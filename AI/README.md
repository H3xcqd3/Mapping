# AI prompt library

Store only successful, reviewed prompts here. Each prompt should state its purpose, inputs, output type, model/tool if relevant, date, and known limitations. Generated output is not automatically approved game content.

Categories: `Terrain`, `Buildings`, `Textures`, `Skyboxes`, `ConceptArt`, `LoadingScreens`, `Icons`, and `Scripts`.
